import styled from '@emotion/styled';

export const IssueWrapperLayer = styled.section`
  width: 80rem;
  border: 1px solid;
  border-radius: 1rem;
  border-color: ${({ theme }) => theme.color.line};
  overflow: hidden;
`;
